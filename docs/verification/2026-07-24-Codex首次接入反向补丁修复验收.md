# Codex 首次接入反向补丁修复验收

日期：2026-07-24
范围：Codex 首次接入的连接计划与反向投影

## 结论

通过。首次接入时缺少 `model_providers.tokenstation` 的 Codex TOML 现在可以生成连接计划；计划中的反向补丁会移除本次新建的 `/model_providers/tokenstation`，并通过现有的受管字段恢复校验。

## 回归证据

新增测试：

```text
agent_integration::plan::tests::codex_first_connection_without_tokenstation_provider_builds_reversible_plan
```

它构造一个含 `model` 和非受管 `approval_policy`、但没有 Token Station provider 的 Codex 配置，验证：

1. `build_connection_plan` 可成功签发计划；
2. 计划包含 `Remove /model_providers/tokenstation` 的反向脱敏变化。

修复前，该测试稳定失败并返回：

```text
Connector 反向 patch 无法恢复接管前受管字段
```

## 验证命令

```text
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --lib -- --test-threads=1
结果：186 passed，1 ignored，0 failed

cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets -- -D warnings
结果：通过，0 warning

rustfmt --edition 2021 --check \
  src/agent_integration/config_codec.rs \
  src/agent_integration/connectors/codex.rs \
  src/agent_integration/connectors/mod.rs \
  src/agent_integration/plan.rs
结果：通过

git diff --check
结果：通过
```

说明：从干净 `origin/develop` 建立的隔离 worktree 中，全量 `cargo fmt --check`
仍会提示基线已有的 `commands.rs` 格式差异；本次未修改该文件，故未把无关格式化带入 PR。
