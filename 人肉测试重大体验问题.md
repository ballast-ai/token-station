# Token Station 人肉测试重大体验问题

> 首次测试日期：2026-07-22
> 最新复测日期：2026-08-11
> 最新复测对象：实际安装的 `/Applications/token-station.app` 与本机 OpenCode `1.18.13`
> 测试方式：测试人员反馈、真实旧配置恢复、受控自动化测试、桌面构建审计、真实 App 重启与 OpenCode 接入验收。

本文只保留当前版本仍未完全解决、且影响用户理解、关键操作或配置安全的重大体验问题。已解决问题由 Git 历史保留。

## 最新复测结论

当前没有仍未解决的重大体验问题。

本轮已关闭 UX-005、UX-006 和 UX-007：

- 本地入站转换失败会明确说明请求尚未发往上游，不再与 `attempts=0` 冲突。
- Anthropic 服务端工具在非原生 Provider 上会于上游前返回明确能力错误，不再静默降级成没有执行者的空函数；原生 Anthropic 链路继续原样透传。
- OpenCode 接入会写入当前有效路由可证明的安全 `limit` 和价格。真实 Wecoding `glm-5.2` 验收值为 `context=257550`、`output=32768`、`input=0.2`、`output=0.6`、`cache_read=0.04`，代理 `/models` 返回一致数据。
- 旧配置中的 `profiles: null` 可以恢复为空 Map，App 重启后不再进入只读保护；旧 OpenCode 配置中的 `provider.tokenstation: null` 也可由一键接入安全修复。

自动化验证包括 Rust workspace 全量测试、桌面前端 287 项测试、桌面 Rust 257 项测试、生产构建、官方本地桌面构建和产物审计。真实 App 验收还覆盖了退出后重新打开、重新启动代理、访问全局路由、刷新模型目录、重新接入 OpenCode，以及重启后再次读取带元数据的 `/agents/opencode/v1/models`。

OpenCode 的真实长会话自动压缩仍需要测试人员在接近阈值时观察，但此前阻断主动压缩的零上下文窗口已经消除。这属于后续运行观察，不再作为当前已知产品缺陷保留。
