# Token Station 人肉测试重大体验问题

> 首次测试日期：2026-07-22  
> 最新复测日期：2026-08-04  
> 最新复测对象：实际运行的 `codex/workbuddy-compat` 桌面 App，基础 commit `80d2ce8` 加 Claude Desktop 模型发现修复，running revision `110`  
> 测试方式：人工体验、本机请求回执与日志只读检查、当前源码审计、隔离工作树回归测试、Claude Desktop 1.24012.11 真实连接检查。

本文只保留当前版本仍未完全解决、且影响用户理解、关键操作或配置安全的重大体验问题。已经解决的问题从当前文档移除，由 Git 历史保留。本轮只调查和记录，没有修改业务代码、真实 Agent 配置或 Token Station 配置。

## 最新复测结论

| 问题 | 最新状态 | 结论 |
|---|---|---|
| 请求回执把本地入站归一化失败误报为“上游拒绝” | 未解决 | 回执已明确显示 `attempts=0` 且 `inbound_normalize` 失败，但诊断文案仍宣称上游拒绝，并且没有保留可安全展示的具体失败原因 |
| Claude Code WebSearch 被当成客户端函数工具解析 | 未解决 | Anthropic 服务端 Web Search 工具按官方协议不带 `input_schema`，Token Station 因此在本地直接返回 400；成功对照发生在 CC Switch → DeepSeek 链路，现有证据更支持 CC Switch 宽松转发 Claude Code 的嵌套搜索请求，而不是 CC Switch 自己执行或伪造搜索 |

与当前回执界面相关的 `RecentReceipts.test.tsx` 在隔离临时副本中通过现有 `6` 项测试。这些测试只证明现有展开、决策、尝试和转换时间线正常渲染，没有覆盖“本地入站失败不得写成上游拒绝”这项语义，所以新问题仍为未解决。

2026-08-04 复测时还发现 Claude Desktop 的 Gateway 模型发现会把 Token Station 返回的 DeepSeek、GLM 和 Ollama 原始模型名全部过滤，显示 `Gateway returned no usable models`。本轮已在隔离工作树修复并更新本机 App，Claude Desktop 的 `Test model discovery` 真实检查显示 `found 1 model auto`，因此不把它作为未解决问题保留。继续执行推理检查时，Claude 已进入 `/v1/messages`，随后因当前 `wec` 上游没有保存 `provider_api_key` 返回 401；本地 `secrets.json` 自 2026-07-31 起只含 DeepSeek 凭据，所以这是独立的供应商配置缺失，不是模型发现修复或本次安装造成的凭据丢失。

## UX-005：请求回执把本地入站归一化失败误报为上游拒绝

### 基本信息

- **问题类型**：诊断层级错误 / 错误信息不可操作 / 回执证据不足
- **影响位置**：最近请求回执详情中的 `DIAGNOSIS`、`DECISION`、`ATTEMPTS` 和 `CONVERSIONS`
- **影响协议**：`anthropic-messages → token-station-chat`
- **影响 Agent**：Claude Code
- **严重程度**：中（P2）
- **状态**：未解决

### 用户侧现象

失败回执展开后显示：

```text
DIAGNOSIS
请求
请求格式不合法，上游拒绝处理。
下一步：检查客户端参数和所选协议。

DECISION
没有决策记录

ATTEMPTS
没有真实上游尝试

CONVERSIONS
1
inbound_normalize
anthropic-messages → token-station-chat
invalid_request
```

这几段信息互相冲突。`DECISION` 和 `ATTEMPTS` 表明请求还没有进入路由，也没有发往任何上游；`CONVERSIONS` 又明确指向本地 `inbound_normalize` 失败。但最上方的诊断却写成“上游拒绝处理”，会让用户去排查 Provider、API Key、额度或网络，而真正失败发生在 Token Station 本地适配器中。

### 实际运行证据

只读统计本机 `requests.log` 中 running revision `79` 的记录：

- 2026-07-30 18:10:51 至 18:14:54（Asia/Shanghai）之间，共有 `82` 条 `HTTP 400 / invalid_request` 记录。
- 这 `82` 条均为 `attempts=0`，没有路由决策和真实上游尝试。
- 唯一转换记录均是 `inbound_normalize` 失败，源协议为 `anthropic-messages`，目标协议为 `token-station-chat`。
- 同一 running revision 期间还有 `251` 条 HTTP 200 记录，其中多条 Claude Code 请求成功通过同一 Anthropic 入站协议进入 `wec / glm-5.2`。

因此，本问题不是“整个 Anthropic 协议或上游不可用”。当前证据只能说明某一类 Claude Code 请求在本地归一化时被拒绝。

### 根因链路

#### 1. 诊断文案只根据顶层错误码生成

位置：

```text
apps/desktop/src/errors.ts:9-38
apps/desktop/src/components/RecentReceipts.tsx:110-123
```

`RecentReceipts` 只把 `receipt.error_code` 传给 `humanizeErrorCode()`。`invalid_request` 在中文映射中被固定解释为：

```text
请求格式不合法，上游拒绝处理。
```

这个映射没有读取失败阶段、是否已产生路由决策、上游尝试数或适配器的拒绝原因。同一个 `invalid_request` 既可能来自本地 JSON 解析和入站归一化，也可能来自路由前校验；用一句“上游拒绝”概括所有情况，必然会误报。

#### 2. 回执不保留有限、可分类的失败原因

位置：

```text
crates/metrics/src/lib.rs:166-178
crates/metrics/src/lib.rs:180-228
apps/cli/src/store.rs:1163-1199
```

`RequestRecord` 和 `ConversionRecord` 只保留 `ErrorCode`、成功与否和协议名，明确不保留任意诊断文本。这条隐私红线本身是对的，但当前数据模型也没有一个有限枚举的失败原因，例如：

```text
missing_model
missing_max_tokens
missing_messages
invalid_message_role
unsupported_thinking
invalid_json
```

所以回执只知道“入站归一化失败”，不知道“为什么失败”。

#### 3. Anthropic 适配器在进入路由前会执行多项本地校验

位置：

```text
plugins/official/agent-anthropic/src/lib.rs:695-760
```

适配器会在 `normalize_inbound()` 中校验 `thinking`、`tool_choice`、`model`、`max_tokens`、`messages`、`tools` 和其他 Anthropic 字段。任意一项失败都会在路由决策和 Provider 请求之前返回 `invalid_request`。

当前日志没有请求正文，回执也没有安全失败原因，因此无法从现有证据确定这 `82` 条请求究竟缺少哪个字段。具体字段原因标记为 **待验证**，不根据空的 `requested_model` 字段直接推断为缺少 `model`：当前实现本来就是在归一化成功后才写入 `requested_model`。

### 影响

1. 用户会把本地协议转换问题当成 Provider 拒绝，去排查与本次失败无关的 API Key、额度、Base URL 和网络。
2. 开发者拿到 request ID 后仍无法从回执数据判断是缺字段、字段类型错误还是能力不支持。
3. 大量这类失败与正常请求交错出现时，用户无法判断它是主任务失败、Claude Code 辅助请求失败，还是不影响对话的噪声。

### 可能的修复方向

#### A. 诊断必须优先使用失败阶段

当第一个失败转换阶段为 `inbound_normalize`、决策为空且尝试数为 0 时，界面应显示：

> 请求在 Token Station 本地转换 Anthropic Messages 格式时失败，尚未发送到上游。

不应出现“上游拒绝”。

#### B. 保留有限、无正文的诊断代码

在不记录请求正文、字段值、密钥和任意适配器文本的前提下，为入站适配器增加稳定的有限失败枚举。回执可以显示字段名和错误类型，但不得显示字段值。

#### C. 空状态要解释为什么没有后续记录

`DECISION` 建议显示“请求在入站转换阶段失败，未进入路由决策”；`ATTEMPTS` 建议显示“请求未发送到任何上游”。这比两句孤立的“没有”更能帮助用户理解时间线。

#### D. 对高频失败指明请求类型

如果这些请求来自 Claude Code 的辅助或后台操作，回执应在不保留正文的前提下记录稳定请求类型或安全路径分类。用户至少需要知道失败是否影响主对话。

### 安全与数据红线

1. 不记录或展示请求正文、system prompt、消息内容、tool input 或图片。
2. 不记录或展示 API Key、虚拟 Key、Authorization header 或其他凭据。
3. 不直接持久化插件返回的任意错误文本，避免请求内容通过错误消息进入日志。
4. 诊断代码必须是 host 验证的闭集枚举，长度和基数都要有上限。

### 验收标准

1. `inbound_normalize` 失败且 `attempts=0` 时，诊断明确说明请求没有到达上游。
2. 界面不再把本地 JSON 解析、入站归一化或路由前校验失败统一写成“上游拒绝”。
3. 回执能用稳定枚举区分至少下列情况：JSON 无法解析、缺少必填字段、字段类型错误、能力不支持、无可用路由、上游拒绝。
4. 若失败与某个字段有关，可以显示字段名和错误类型，但不显示字段值或任何请求内容。
5. `DECISION` 和 `ATTEMPTS` 的空状态说明是哪个更早阶段阻止了后续流程。
6. 同一个 `invalid_request` 在本地校验失败和真实上游返回时，显示不同的诊断层级和下一步。
7. 回归测试覆盖 Anthropic 缺少 `model`、缺少 `max_tokens`、缺少 `messages`、不支持的 `thinking` 以及真实上游 400，并验证每种情况的时间线。
8. 测试使用临时配置、临时数据库和虚构请求，不读写用户的 Claude Code 会话、Token Station 配置或真实凭据。

---

## UX-006：Claude Code WebSearch 无法穿过 Token Station 的 GLM 路由

### 基本信息

- **问题类型**：Anthropic 服务端工具不兼容 / 协议语义丢失 / 能力准入缺失
- **影响位置**：Claude Code `WebSearch`，以及依赖 WebSearch 的 deep-research 工作流
- **入站协议**：`anthropic-messages`
- **当前上游**：经 Token Station 路由到 OpenAI-compatible `wec / glm-5.2`
- **用户实测错误**：`API Error: 400 tool declares no input_schema`
- **严重程度**：高（P1）
- **状态**：未解决
- **关联问题**：UX-005 把这类本地归一化失败误报为“上游拒绝”

### 用户侧现象

Claude Code 在 Token Station 代理下可以正常对话，普通工具也能被 GLM 调用。但一旦进入 `WebSearch`，请求立即失败：

```text
API Error: 400 tool declares no input_schema
```

本次实测中，依赖 WebSearch 的 deep-research 工作流无法继续，只能停止任务，否则会持续重试并消耗 token。

Claude Code `2.1.173` 的本地会话证据还显示，GLM 曾正常产生名为 `WebSearch` 和 `WebFetch` 的 `tool_use`。WebSearch 的问题不是模型完全不会选工具，而是 WebSearch 进入后续 Anthropic 服务端搜索请求时，被 Token Station 的入站转换拒绝。

### 对照复测：切换到 CC Switch 3.19.0 后 WebSearch 恢复

2026-07-30 在同一台机器、同一 Claude Code `2.1.173` 环境中切换代理后，WebSearch 可以连续返回真实搜索结果。复测时观察到：

- Token Station 桌面进程仍在运行，但 `127.0.0.1:8787` 已没有监听端口。
- CC Switch `3.19.0` 进程监听 `127.0.0.1:15721`。
- CC Switch 的 `/health` 无需鉴权即可返回 HTTP 200 和 `healthy`，`/v1/models` 返回 HTTP 200 和空模型数组 `{"models":[]}`。
- 最新 Claude Code 会话记录的模型为 `deepseek-v4-pro`。多次 `WebSearch` 的 `tool_result` 均为成功状态，结果中包含多个外部 URL；本记录没有保存查询文本和搜索正文。
- 用户使用同一个只含 `type`、不含 `input_schema` 的 Web Search 服务端工具请求分别直连两个本地代理。Token Station 返回 HTTP 400 `tool declares no input_schema`；CC Switch 返回 HTTP 402 `Insufficient Balance`。
- CC Switch 本地日志在相同时间段明确记录请求目标为 `https://api.deepseek.com/anthropic/v1/messages`、模型为 `deepseek-v4-pro`，随后由转发器记录 `Provider DeepSeek 请求失败: 上游 HTTP 402: Insufficient Balance`。Claude Code 会话也记录了相同的 HTTP 402。

这组 A/B 对照说明，Claude Code 本身及 WebSearch 工作流不是共同故障点。绕开 Token Station 后功能立即恢复，而 Token Station 路径在 `inbound_normalize` 阶段稳定返回自己的 `tool declares no input_schema`，因此当前故障边界可以收敛到 Token Station 的 Anthropic 工具解析与后续能力模型。

这次 400 对 402 的对照进一步证明，CC Switch 没有在本地执行 Token Station 这条“每个工具必须带 `input_schema`”的严格校验，并且请求确实到达了 DeepSeek 的 Anthropic 接口。它的可观察设计更接近宽松转发：不认识的工具形状至少不会在本地直接拒绝；Token Station 则先把 Anthropic 请求解析成受限的 Canonical IR，再重建上游请求，未知形状会在解析阶段失败。

### 成功搜索的真实链路：不是 GLM，也没有证据表明 CC Switch 自己搜索

Claude Code 会话时间线显示，切换代理前最后一组 GLM 请求在 2026-07-30 18:23:16 发出 `WebSearch`，随后返回错误结果；切换后从 18:23:40 开始，发出 `WebSearch` 的外层模型已是 `deepseek-v4-pro`。18:23:54 和 18:24:23 等多次对应 `tool_result` 成功返回，并包含真实 URL。因此，当时成功的搜索发生在 CC Switch → DeepSeek 链路，不是当前 Token Station → GLM 链路。

Claude Code `2.1.173` 二进制中的 WebSearch 实现字符串进一步揭示了两层调用：

1. 外层 `deepseek-v4-pro` 先返回普通客户端 `tool_use`，名称为 `WebSearch`。外层模型消息中的 `server_tool_use.web_search_requests` 为 0，因为这条消息本身没有直接执行 Anthropic 服务端搜索。
2. Claude Code 的 WebSearch 工具随后启动一个专门用于搜索的内部请求。二进制中同时出现 `You are an assistant for performing a web search tool use`、`web_search_20250305`、`server_tool_use`、`web_search_tool_result` 和搜索进度事件解析逻辑。
3. CC Switch 在同一时间段持续把 Claude 请求转发到 `https://api.deepseek.com/anthropic/v1/messages`。DeepSeek 官方的 [Claude Code 接入文档](https://api-docs.deepseek.com/guides/agent_integrations/claude_code) 也将 `https://api.deepseek.com/anthropic` 列为 Anthropic-compatible Base URL；公开文档没有单独说明 `web_search_20250305` 的支持范围。
4. Claude Code 把内部搜索响应整理成外层 `WebSearch` 的普通 `tool_result`，再交还给 `deepseek-v4-pro` 继续生成答案。

按现有证据，最合理的执行链是“DeepSeek 决定调用 WebSearch → Claude Code 发起嵌套服务端搜索请求 → CC Switch 转发到 DeepSeek Anthropic 接口 → Claude Code 收到搜索结果并回填”。CC Switch 在这里负责接管配置和转发，没有证据表明它通过 prompt 注入、缓存或自带搜索 API 执行搜索。

此前用于支持“CC Switch 认识并执行 WebSearch”的二进制字符串也被误读了。`web_search_tool_type: text_and_image` 与 `apply_patch_tool_type: freeform` 位于 CC Switch 内置的 Codex `gpt-5.5` 模型目录配置中，用于描述 Codex 工具类型，不是 Claude/DeepSeek 搜索执行器。CC Switch 二进制中没有检出 `web_search_20250305`、`web_search_tool_result` 或 `server_tool_use` 的专门处理字符串。

仍有一个证据缺口：CC Switch 日志没有保存脱敏的出站请求体和上游搜索响应，因此无法从现有历史记录证明它对 `web_search_20250305` 完全逐字段原样透传，也无法区分 DeepSeek Anthropic 接口是原生执行搜索，还是在其服务端兼容层转换后执行。后续 HTTP 402 只证明请求到达 DeepSeek 并在余额阶段失败，不能单独证明工具 schema 已通过校验。

CC Switch 的开放 `/health` 和空 `/v1/models` 是本轮对照观察，不足以单独判定安全问题或模型发现缺陷，也不纳入 Token Station 的新问题。本次切换只是绕过故障，UX-006 在 Token Station 中仍为未解决。

### 直接原因：Anthropic 适配器强制每个工具携带 `input_schema`

位置：

```text
plugins/official/agent-anthropic/src/lib.rs:394-417
```

`parse_tools()` 把 Anthropic `tools` 数组的每一项都解析为 `ToolDef`，并无条件要求：

```rust
parameters: tool
    .get("input_schema")
    .cloned()
    .ok_or_else(|| invalid("tool declares no input_schema"))?,
```

因此，报错文本是 Token Station 自己产生的。请求在 `inbound_normalize` 阶段就返回 HTTP 400，没有进入路由决策，也没有发往 GLM 或其他 Provider。

### 官方协议证据：Web Search 本来就没有 `input_schema`

Anthropic 官方 [Web Search tool 文档](https://platform.claude.com/docs/en/agents-and-tools/tool-use/web-search-tool) 将 Web Search 定义为由 Anthropic 基础设施执行的服务端工具。官方请求示例为：

```json
{
  "type": "web_search_20250305",
  "name": "web_search",
  "max_uses": 5
}
```

这种工具用 `type` 指定由 Anthropic 服务端执行的能力，不是由客户端自行实现的 JSON Schema 函数，所以不携带 `input_schema` 是合法且预期的协议形态。

Anthropic 官方 [Tool use overview](https://platform.claude.com/docs/en/agents-and-tools/tool-use/overview) 也区分了两类工具：用户定义的客户端工具需要 `input_schema`；`web_search`、`web_fetch`、`code_execution` 和 `tool_search` 等服务端工具由 Anthropic 的基础设施运行。

### 更深根因：Token Station 的 Canonical IR 只能表示客户端函数工具

#### 1. `ToolDef` 没有服务端工具类型

位置：

```text
crates/protocol/src/chat.rs:112-120
```

当前 `ToolDef` 只有：

- `name`
- `description`
- 必填 `parameters`

它没有工具执行位置、服务端工具 `type`、`max_uses`、域名限制、`allowed_callers` 或结果引用语义。因此即使 `agent-anthropic` 不在第 413 行报错，它也无法把 `web_search_20250305` 无损放入现有 Canonical IR。

#### 2. OpenAI-compatible 出站会把所有工具渲染为客户端函数

位置：

```text
plugins/official/provider-openai-compatible/src/lib.rs:207-222
```

出站适配器对每个 `ToolDef` 统一生成：

```json
{
  "type": "function",
  "function": {
    "name": "...",
    "description": "...",
    "parameters": {}
  }
}
```

对 `wec / glm-5.2` 来说，这只能表示“让模型返回一个由客户端执行的函数调用”，无法表示“让 Anthropic 服务端执行搜索并返回带引用的 `web_search_tool_result`”。

#### 3. Responses 适配器已明确承认同一架构边界

位置：

```text
plugins/official/agent-openai-responses/src/lib.rs:367-401
```

Responses 入站适配器的注释明确写明，Canonical IR 只有 `function` 工具的表示；`web_search`、`file_search`、`computer`、`code_interpreter` 和 MCP 等服务端或内建工具“没有 IR shape”。当前 Responses 路径选择静默丢弃这些工具，Anthropic 路径则把它们误当成缺少 schema 的函数工具并拒绝整个请求。两条路径都没有真正支持服务端工具。

### 为什么 WebFetch 可以用

在当前 Claude Code `2.1.173` 的实际会话中，`WebFetch` 以普通 `tool_use` 返回给 Claude Code，同一消息的 `server_tool_use.web_fetch_requests` 仍为 0。随后网页获取由 Claude Code 客户端流程执行。这与 WebSearch 需要发起 Anthropic 服务端 Web Search 请求的路径不同，所以它不会在同一位置因缺少 `input_schema` 失败。

这一点是根据当前版本的会话结构和运行结果得出的推断，不代表 Claude Code 以后的所有版本都会保持相同实现。

### 结论

1. **直接故障不在 GLM 上游**：当前 WebSearch 请求在 Token Station 的 `inbound_normalize` 阶段就被拒绝，根本没发到 GLM。
2. **但路由到 GLM 是能力缺失的根本背景**：Anthropic 原生 Web Search 依赖 Anthropic 服务端执行。当请求被改道到普通 OpenAI-compatible GLM 上游时，Anthropic 的搜索服务本来就不在这条数据路径上；第三方代理若要让它可用，必须额外提供等价执行桥或明确的转换流程。
3. **不能只删除 `input_schema` 校验**：那样只会把一个服务端工具伪装成客户端函数，丢失 `type`、搜索执行、结果块、使用量和引用语义。
4. **CC Switch 对照确认了 Token Station 的故障边界**：成功搜索时，外层模型是 DeepSeek，Claude Code 负责调度 WebSearch 和回填 `tool_result`，CC Switch 将相关请求发往 DeepSeek Anthropic 接口。现有证据不支持“CC Switch 自己执行或伪造搜索”，但仍不能证明工具字段逐字段原样透传。

### 可能的修复方向

#### A. 先做诚实的能力拦截

在未实现服务端工具之前，`agent-anthropic` 应识别 `web_search_20250305` 等已知服务端工具，返回结构化 `capability` 错误，明确说明：

> 当前 GLM 路由不支持 Anthropic 服务端 Web Search。请使用支持该能力的原生 Anthropic 路由，或关闭依赖 WebSearch 的工作流。

不应继续返回“缺少 `input_schema`”，也不应把工具静默丢弃后声称请求完整。

#### B. 在 Canonical IR 中区分客户端函数和服务端工具

如果产品要真正支持 WebSearch，工具模型至少需要区分：

- 客户端函数工具：保留 `input_schema`，由 Claude Code 或其他客户端执行。
- 服务端工具：保留协议 `type`、执行位置、配置项、结果类型、使用量和引用信息。

路由器必须把“需要服务端 Web Search”当成模型与 Provider 能力条件，不得把请求送到不能执行或无损传递该工具的上游。

#### C. 选择真正的执行方案

支持 WebSearch 至少有两条路，两者的成本和数据边界完全不同：

1. 对原生 Anthropic Provider 无损传递 Anthropic 服务端工具，并完整翻译 `server_tool_use`、`web_search_tool_result`、引用和使用量。
2. 由 Token Station 提供自己的搜索执行器，把结果安全转换回 Anthropic 语义。这会引入搜索供应商、凭据、域名策略、费用、隐私、引用正确性和 SSRF 防护等新边界，不能当成一个 schema 兼容小修补。

### 不建议的修复方式

1. **不要给 WebSearch 硬塞空 `input_schema`**：这会把服务端工具伪装成普通函数，但没有任何一层真正执行搜索。
2. **不要照搬 Responses 适配器的静默丢弃做法**：对主动调用 WebSearch 的工作流来说，静默降级会使输出缺少搜索证据，却看起来像正常完成。
3. **不要把问题归因为 GLM 生成质量**：目前的 400 发生在模型请求之前。

### 安全与数据红线

1. 不记录 WebSearch 查询文本、搜索结果正文、system prompt 或完整工具参数。
2. 不在回执、普通日志或错误消息中记录 Anthropic、搜索供应商或 Token Station 凭据。
3. 未通过能力准入时 fail closed，不把服务端工具改写成语义不等价的客户端函数。
4. 如果 Token Station 自行执行搜索，必须对 URL、重定向、私有网段、DNS rebinding、响应体积、超时、并发和域名策略做独立安全设计。

### 验收标准

1. 包含 `web_search_20250305` 的合法 Anthropic Messages 请求不再返回 `tool declares no input_schema`。
2. 当当前 Provider 无法执行服务端 Web Search 时，请求在路由前返回结构化 `capability` 错误，页面和 Claude Code 能看到可操作原因。
3. 只有明确声明并通过验证的 Provider 或 Token Station 执行器才能承接服务端工具；普通 OpenAI-compatible GLM 不得被乐观判定为支持。
4. 真正支持时，WebSearch 的调用、结果块、使用量、错误和引用都能完整往返，不被改写成无执行者的函数调用。
5. WebFetch 等现有客户端工具不受影响，混合工具数组中一个不支持的服务端工具不得被误报为客户端 schema 缺失。
6. 回归测试覆盖纯客户端函数、纯 `web_search_20250305`、混合工具、支持与不支持的 Provider，以及搜索错误和引用翻译。
7. 回执只记录有限的工具类型、执行位置、阶段和错误码，不记录查询或搜索结果正文。
8. 使用同一 Claude Code 环境做跨代理 A/B 验证时，Token Station 必须明确显示真实执行路径；成功不能只由最终文本推断，还要能确认搜索执行者、结果类型、使用量和引用来源。
