# TokenStationDockDark

Generated from `work/spec.json` in this directory.

The historical transform script in `work/prepare-dark-icon.mjs` requires the `pngjs` package.

- Use `token-station-dock-dark.preview.svg` and `token-station-dock-dark.preview.png` for rounded local review.
- Use `token-station-dock-dark.svg` and `token-station-dock-dark.png` for an unrounded source/app canvas.
- Generic source assets can be adapted to a requested platform after review.
